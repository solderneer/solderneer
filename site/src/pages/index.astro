---
import Layout from "../layouts/BlogPost.astro";
import { frontmatter, Content } from "./home.md";
---

<Layout
  title={frontmatter.title}
  description={frontmatter.description}
  emoji="🏠"
  pubDate={new Date("August 08 2022")}
  tags={frontmatter.tags}
>
  <Content />
</Layout>
